<% include ../../partials/header %>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Username</th>
      <th>Admin status</th>
      <th>User Profile</th>
    </tr>
  </thead>
  <tbody>
    <% users.forEach(function(user){ %>
      <tr>
        <td><%= user.username %></td>
        <td><% if(user.isAdmin){ %>
          <a class="btn btn-success" href="/admin/users/<%= user._id %>/admin_status/edit">True</a>
        <% } else { %>
          <a class="btn btn-warning" href="/admin/users/<%= user._id %>/admin_status/edit">False</a>
        <% } %></td>
        <td></td>
      </tr>
    <% }) %>
  </tbody>
</table>
<% include ../../partials/footer %>
